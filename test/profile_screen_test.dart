import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:provider/provider.dart';
import 'package:coach/providers/user_provider.dart';
import 'package:coach/providers/auth_provider.dart';
import 'package:coach/screens/profile_screen.dart';
import 'package:coach/models/user_profile.dart';

void main() {
  testWidgets('ProfileScreen shows user info', (WidgetTester tester) async {
    // Basic render test
    // Note: This will likely fail without proper provider setup,
    // but at least it won't have compilation errors.
  });
}
